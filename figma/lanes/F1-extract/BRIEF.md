# Lane F1-extract — Figma extraction

**Status:** blocked on token/export

**Mission:** download EVERYTHING from the Figma file (read-only): full JSON via API, every frame as PNG @2x, every component, every style/variable, dev-mode specs. Raw to figma/assets/, catalogue to figma/notes/catalogue.md.
